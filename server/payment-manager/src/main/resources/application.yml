server:
    port: 9005

spring:
    application:
        name: payment-manager-service

eureka:
    client:
        registerWithEureka: true
        fetchRegistry: true
        ServiceUrl:
            defaultZone: http://localhost:8761/eureka
    instance:
        hostname: localhost